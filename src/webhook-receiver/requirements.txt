# 1.24 is the current stable v1 library line compatible with the Python 3.11
# Functions runtime.  Keep it explicit so remote builds index decorators with
# the same SDK contract as local validation.
azure-functions==1.24.0
azure-servicebus==7.12.3
azure-identity==1.17.1
azure-data-tables==12.6.0
azure-storage-blob==12.24.1
